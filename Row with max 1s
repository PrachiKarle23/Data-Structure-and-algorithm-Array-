int rowWithMax1s(vector<vector<int> > arr, int n, int m) {
	    int r=0,c=m-1;
	    int maxi=-1;
	    while(r<n && c>=0)
	    {
	        if(arr[r][c]==1){
	            maxi=r;
	            c--;
	        }
	        else{
	            r++;
	        }
	    }
	    return maxi;
	}
